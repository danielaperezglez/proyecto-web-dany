<div class="container-fluid">
    <div class="row mb-4">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center">
                <h2>🏷️ Gestión de Roles</h2>
                <div>
                    <a href="<?= PUBLIC_PATH ?>usuarios" class="btn btn-outline-secondary me-2">
                        👥 Usuarios
                    </a>
                    <a href="<?= PUBLIC_PATH ?>roles/crear" class="btn btn-primary">
                        ➕ Nuevo Rol
                    </a>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead class="table-dark">
                                <tr>
                                    <th>ID</th>
                                    <th>🏷️ Rol</th>
                                    <th>📝 Descripción</th>
                                    <th>👥 Usuarios</th>
                                    <th>📊 Estado</th>
                                    <th>⚙️ Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                <?php foreach ($roles as $rol): ?>
                                <tr>
                                    <td><?= $rol->id ?></td>
                                    <td>
                                        <div class="d-flex align-items-center">
                                            <span class="me-2" style="font-size: 24px;"><?= $rol->icono ?></span>
                                            <strong><?= ucfirst($rol->nombre) ?></strong>
                                        </div>
                                    </td>
                                    <td><?= $rol->descripcion ?></td>
                                    <td>
                                        <?php 
                                        $usuarios = (new Usuario())->count("rol = '{$rol->nombre}'");
                                        ?>
                                        <span class="badge bg-info"><?= $usuarios ?> usuarios</span>
                                    </td>
                                    <td>
                                        <?php if ($rol->activo): ?>
                                            <span class="badge bg-success">✅ Activo</span>
                                        <?php else: ?>
                                            <span class="badge bg-warning">⚠️ Inactivo</span>
                                        <?php endif; ?>
                                    </td>
                                    <td>
                                        <div class="btn-group" role="group">
                                            <a href="<?= PUBLIC_PATH ?>roles/editar/<?= $rol->id ?>" 
                                               class="btn btn-sm btn-outline-primary" title="Editar">
                                                ✏️
                                            </a>
                                            
                                            <?php if ($rol->nombre !== 'admin'): ?>
                                            <a href="<?= PUBLIC_PATH ?>roles/toggle_estado/<?= $rol->id ?>" 
                                               class="btn btn-sm btn-outline-<?= $rol->activo ? 'warning' : 'success' ?>" 
                                               title="<?= $rol->activo ? 'Desactivar' : 'Activar' ?>"
                                               onclick="return confirm('¿Cambiar estado del rol?')">
                                                <?= $rol->activo ? '🚫' : '✅' ?>
                                            </a>
                                            
                                            <?php 
                                            $usuarios = (new Usuario())->count("rol = '{$rol->nombre}'");
                                            if ($usuarios == 0): 
                                            ?>
                                            <a href="<?= PUBLIC_PATH ?>roles/eliminar/<?= $rol->id ?>" 
                                               class="btn btn-sm btn-outline-danger" title="Eliminar"
                                               onclick="return confirm('¿Eliminar rol permanentemente?')">
                                                🗑️
                                            </a>
                                            <?php endif; ?>
                                            <?php endif; ?>
                                        </div>
                                    </td>
                                </tr>
                                <?php endforeach; ?>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>