<?= Form::open('session/login') ?>
    <div class="form-floating">
        <?= Form::email('login[email]', ['class' => 'form-control', 'id' => 'email', 'placeholder' => 'correo@ejemplo.com', 'required' => true]) ?>
        <label for="email">📧 Correo Electrónico</label>
    </div>
    
    <div class="form-floating">
        <?= Form::password('login[password]', ['class' => 'form-control', 'id' => 'password', 'placeholder' => 'Contraseña', 'required' => true]) ?>
        <label for="password">🔑 Contraseña</label>
    </div>
    
    <div class="d-flex justify-content-between align-items-center mb-3">
        <div class="form-check">
            <input class="form-check-input" type="checkbox" id="remember">
            <label class="form-check-label" for="remember">
                Recordarme
            </label>
        </div>
        <a href="#" class="text-decoration-none" style="color: #667eea;">
            ¿Olvidaste tu contraseña?
        </a>
    </div>
    
    <div class="d-grid">
        <?= Form::submit('🚀 Iniciar Sesión', ['class' => 'btn btn-login']) ?>
    </div>
<?= Form::close() ?>