<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset=<?= APP_CHARSET ?> >
    <title>KumbiaPHP Framework</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <?php Tag::css('kumbia') ?>
    <?= Html::includeCss() ?>

    <!-- Bootstrap principal -->
    <link href="/kumbiaphp/libs/bootstrap/bootstrap.min.css" rel="stylesheet">

    <!-- ✅ Tu hoja de estilos personalizada debe ir DESPUÉS de Bootstrap -->
    <link href="<?= PUBLIC_PATH ?>css/custom.css" rel="stylesheet">
</head>

AQUI

<body>
<div class="container">
    <header class="row">
        <div class="col-8">
            <?= Html::link('', Html::img('kumbiaphp.svg', 'KumbiaPHP logo'), 'title="Inicio"') ?>
        </div>
        <div class="col-4 text-right">
            App: <strong><?= basename(dirname(APP_PATH)) ?></strong>
            (<?= PRODUCTION ? 'Production' : 'Development' ?>)
        </div>
    </header>

    <?php View::partial('Menu') ?>
    <?php View::content() ?>
    <?php View::partial('kumbia/footer') ?>
</div>

<script src="/kumbiaphp/libs/bootstrap/bootstrap.bundle.min.js"></script>
</body>
</html>

